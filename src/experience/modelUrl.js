/**
 * Single shared URL so every consumer hits the same cache entry —
 * mismatched query strings here previously downloaded the GLB twice.
 */
export const MODEL_URL = "/Try1.glb?v=13";
export const MODEL_USE_DRACO = false;
export const MODEL_USE_MESHOPT = false;
